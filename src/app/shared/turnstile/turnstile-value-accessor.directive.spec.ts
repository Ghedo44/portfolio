import { TurnstileValueAccessorDirective } from './turnstile-value-accessor.directive';

describe('TurnstileValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnstileValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
